{
  "name": "@houston-ai/engine-protocol",
  "version": "0.1.0",
  "description": "Wire protocol for the TypeScript Houston Engine: REST DTOs, the WebSocket envelope, the HoustonEvent + FeedItem unions, and zod request schemas. Hand-mirrors engine/houston-engine-protocol + houston-ui-events + houston-terminal-manager. The Rust side stays the source of truth.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "files": [
    "src"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
